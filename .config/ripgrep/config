# Ignore files larger than 10M
--max-filesize=10M

# Prevent ripgrep to print really long lines and show a preview
--max-columns=150
--max-columns-preview

# Search hidden files / directories (e.g. dotfiles) by default
--hidden

# Using glob patterns to include/exclude files or folders
--glob=!.git/*

# Set the colors.

--colors=line:fg:120,169,255
--colors=line:style:bold
--colors=path:fg:190,149,255
--colors=match:fg:22,22,22
--colors=match:bg:66,190,101
--colors=match:style:bold

# Search case insentitively if pattern is all lowercase otherwise search case sensitively
--smart-case
