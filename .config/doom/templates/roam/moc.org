#+begin_quote
This map organises concepts related to *${title}* -- %^{Scope, one line}.
#+end_quote

* Landscape
%?

# A MoC is pure navigation. Prefer a hub atomic note for a focused topic; use
# this only when a domain has many top-level notes and no single hub fits.
